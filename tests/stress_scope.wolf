print "Scope Hell Starting..."

let x: int = 0
let target: int = 10000

while x < target
    
    let y: int = x * 2
    let z: int = y + 5
    let cop: string = "this needs to be deleted on memory"
    
    x = x + 1
end

print "Done! if RAM usage not made in it's limits, Garbage Collection (Rust RAII) works."